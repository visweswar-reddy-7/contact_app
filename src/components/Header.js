import React from "react";

const Header = () => {
    return (
        <div className = "ui inverted menu blue item">
            <div className = "ui container center">
                <h2 className="olive item">Contact Manager</h2>
            </div>
        </div>
    );
};

export default Header;
